# OpenWeatherMap API Key
# Safe to leave as placeholder — app uses realistic mock data if no key is set
# Set the real key as an environment variable on Render dashboard instead
OPENWEATHER_API_KEY=your_key_here
